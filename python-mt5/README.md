# MT5 Trading Journal - Python Connector

Скрипт для підключення до MetaTrader 5 та отримання торгової інформації.

## Встановлення

1. Створіть віртуальне середовище:
```bash
python3 -m venv venv
source venv/bin/activate  # На Mac/Linux
# або
venv\Scripts\activate  # На Windows
```

2. Встановіть залежності:
```bash
pip install -r requirements.txt
```

3. Створіть файл `.env` на основі `.env.example`:
```bash
cp .env.example .env
```

4. Відредагуйте `.env` файл та додайте ваші дані:
```
MT5_LOGIN=ваш_номер_акаунту
MT5_PASSWORD=ваш_пароль
MT5_SERVER=назва_сервера_брокера
```

## Використання

Запустіть скрипт:
```bash
python mt5_connector.py
```

## Що робить скрипт

- ✅ Підключається до MT5 акаунту
- 📊 Виводить інформацію про акаунт (баланс, equity, маржа)
- 📈 Показує відкриті позиції
- 📜 Виводить історію угод за останні 7 днів

## Важливо

⚠️ **НІКОЛИ** не комітьте файл `.env` в Git! Він містить ваші особисті дані.
